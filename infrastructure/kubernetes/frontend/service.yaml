apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: atlascore
  labels:
    app: atlascore-frontend

spec:
  type: NodePort

  selector:
    app: atlascore-frontend

  ports:
    - name: http
      port: 3000
      targetPort: http
      nodePort: 30080
      protocol: TCP